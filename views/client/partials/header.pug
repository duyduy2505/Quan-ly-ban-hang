include ../mixins/sub-menu.pug


header.header
  .container
    .row.align-item-center
      .inner-logo
        img(src="/images/logo.png", alt="Logo")
      nav.inner-menu
        ul
          li.sub-menu
            a(href="/") Trang chủ
          li.sub-menu
            a(href="/products") Danh mục sản phẩm
            +sub-menu(layoutProductCategories, '/products')
